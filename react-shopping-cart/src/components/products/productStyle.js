import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(theme => ({
  media: {
    height: 340,
    width: 200,
    // overflow: "hidden",
    // overflow: "hidden",
  },
}));
